{
  "build": {
    "tasks": [
      {
        "name": "Codegen",
        "steps": [{"name": "** Load Unity to generate code **", "command": "cmd", "arguments": ["/k echo No-op."]}]
      },
      {
        "name": "build",
        "steps": [{"name": "** Run ci/build-client.sh instead **", "command": "cmd", "arguments": ["/k echo No-op."]}]
      },
      {
        "name": "clean",
        "steps": [{"name": "No-op", "command": "cmd", "arguments": ["/k echo No-op."]}]
      }
    ]
  },
  "bridge": {
    "worker_attribute_set": {
      "attributes": [
        "DeploymentManager"
      ]
    },
    "component_delivery": {
      "default": "RELIABLE_ORDERED",
      "checkoutAllInitially": true
    }
  },
  "external": {
    "default": {
      "run_type": "EXECUTABLE",
      "windows": {
        "command": "build/worker/DeploymentManager@Windows/DeploymentManager@Windows.exe",
        "arguments": [
          "+workerType",
          "DeploymentManager",
          "+infraServicesUrl",
          "http://127.0.0.1:21000",
          "+projectName",
          "${IMPROBABLE_PROJECT_NAME}",
          "-logfile",
          "../../logs/external-default-unityclient.log"
        ]
      }
    }
  }
}
